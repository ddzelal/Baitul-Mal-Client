{
    "build": {
      "command": "npm install && npm run build",
      "output": "dist"
    },
    "routes": {
      "frontend": {
        "path": "/",
        "static": true,
        "directory": "dist"
      }
    }
  }